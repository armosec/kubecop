apiVersion: kubescape.io/v1
kind: RuntimeRuleAlertBinding
metadata:
  name: all-pods-1
spec:
  namespaceSelector:
  podSelector:
  rules:
    - ruleName: "Unexpected process launched"
      ruleID: "R0005"
    - ruleTags:
      - "exec"
      - "whitelisted"
    
